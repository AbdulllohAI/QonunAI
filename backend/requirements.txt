# ---- web / api -------------------------------------------------------------
fastapi==0.115.6
uvicorn[standard]==0.34.0
python-multipart==0.0.20
pydantic==2.10.4
pydantic-settings==2.7.1
email-validator==2.2.0

# ---- database --------------------------------------------------------------
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
psycopg2-binary==2.9.10
alembic==1.14.0
pgvector==0.3.6

# ---- cache / queue ---------------------------------------------------------
redis==5.2.1
celery==5.4.0

# ---- auth ------------------------------------------------------------------
pyjwt==2.10.1
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# ---- ingestion / parsing ---------------------------------------------------
httpx==0.28.1
beautifulsoup4==4.12.3
lxml==5.3.0
pdfplumber==0.11.5
pypdf==5.1.0
python-docx==1.1.2
pytesseract==0.3.13
Pillow==11.0.0

# ---- language --------------------------------------------------------------
langdetect==1.0.9

# ---- llm providers ---------------------------------------------------------
anthropic==0.42.0
openai==1.59.6

# ---- embeddings / reranking ------------------------------------------------
# Absent from this file until now, while torch was installed and every chunk in
# the corpus carried a bge-m3 vector. The result: documents were embedded but
# queries could not be, so the dense branch raised on every request and the
# hybrid retriever silently ran on keyword search alone.
sentence-transformers==3.3.1
